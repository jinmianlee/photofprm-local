# 混元形状细节实验

## 结论与当前入口

目前没有训练出新的混元权重，也没有证据表明修改几个参数就能让 Mini 普遍达到高精度扫描效果。本轮改变了形状生成输入并运行真实对照，保留有用的局部建模入口，未把效果不稳定的特征混合设为默认。

上传一张照片后，可以选择「整个主体」或「框选部位」。后者将框选的头部等区域交给同一套混元模型**单独生成**。用鼠标/触摸拖出选区，或填写四个百分比边界；选区短边需要至少 256 个原始像素。更换照片会重置选区和材料色。原始上传文件保留，选区先按 EXIF 朝向校正后的原图裁剪，再去背景、推理、上色及分色。不会把放大的图片冒充新的原始细节。

当前人物照片的独立头部中，眉眼、耳廓与头发沟槽比整图模型中的头部更清楚；但眼镜附近仍有模糊和碎边，相似度没有通过用户验收。整体头身比例与身体连接不在这项功能中。部位模型不能直接当成适配原身体的装配零件。

## 已运行的对照

三组均使用本机 Intel Arc XPU、相同官方 Mini Turbo 权重、随机种子 12345、5 步推理和 255 表面提取。没有对比图片润色，也没有用面数当质量分数。

| 方案 | 形状输入 | 结果与判断 |
|---|---|---|
| 原版 | 人物整图 | 基准，路径 `data/2412cfadbbde4c04a068dfd582f05a6a` |
| 局部特征混合，0.35 | 整图 DINO 特征，混入放大头部后映射回原位置的特征 | 241.78 秒。头发纹理改变，脸形、表情及局部身体也改变；没有足够证据说明更像。仅保留实验 CLI，未接入正常按钮。 |
| 独立头部 | 从同一透明前景图裁剪像素框 `[555,130,900,590]`，345 × 460 像素 | 225.53 秒。可见五官和头发结构增加，但没有解决全部相似度问题；可作为独立局部模型使用。 |

这些是各次运行的实测耗时，不能据此承诺速度提升；未做重复计时，也不包含最后的分色处理。

实际几何渲染：

- `data/detail-roi-035/comparison.png`：基准与特征混合，同一相机、取景范围和灯光，包含整体及局部。
- `data/detail-head-only/head-comparison.png`：整图中的头部与独立头部，两侧独立居中等比缩放、相同灯光，未自动配准或拼接。
- `data/detail-head-only/views.png`：独立头部前、侧、后三维渲染。背面由混元推断。
- `data/region-api-validation.json`：浏览器所用 API 的框选生成与下载验证记录，以其中的 `state` 为准。

原始 marching-cubes 网格可能包含退化面，不能直接把原始 STL 当作已验证打印文件。应用会移除零面积/重复面、检查封闭性并整理底面；通过后才分色导出。最终应使用任务的 `output/` 文件。

### 浏览器所用 API 的实际验收

任务 `432ab64605f84744b6c22f0db82c10f8` 已完整运行，上传原图保留，最终使用的透明选区与前述独立头部实验逐字节相同。首次打印检查发现一个三角形缺口，正确停止了导出；开启「更多打印检查参数 → 修复三角形 / 四边形小孔」后恢复任务，复用原始网格，49 秒完成整理及三色导出，没有重跑混元。

本轮接通了小孔修复选项在单图路径中的处理。单图修复只追加三角/四边形小孔补片，最多 32 个三角面，补片边长不超过实际打印尺寸下的 0.8 mm、补片总面积不超过原面积的 0.1%；原有顶点与面保持不变，修后仍必须完全封闭。本例补了一个三角面，最长边 0.5088 mm。小孔修复默认关闭，大范围缺失会继续拒绝。

最终模型为 69.816 × 72.308 × 95 mm；完整 STL 和 3 个材料 STL 均通过下载后重新读取的封闭性、法线、正容积与有限坐标检查，3MF 与 ZIP 可完整读取。42 项回归测试通过，前端 TypeScript 编译和生产构建通过；另有局部特征与断点隔离检查通过。未进行浏览器点击测试或实物打印。

材料分区仍含小孤岛，不能把封闭性检查解释为所有细节都能直接打印；眼镜附近也仍有模糊和碎边。检查切片中的薄壁、支撑与小孤岛后再打印。

## 特征适配实验的边界

`backend/detail_conditioning.py` 将头部区域放大到图像编码器输入尺寸，重新计算 DINO 特征，再按原图坐标映射回原特征网格。混合时保留原 CLS token，区域外的 token 不变，区域边缘渐变混合，特征幅度按原位置 RMS 对齐。仍然使用完整注意力，不增加隐藏的远程服务。

这只约束了**输入特征的变化范围**，不能保证最终三维变化只出现在头部：混元的形状推理会联合处理这些特征。这正是本轮对照中脸形与身体一起变化的原因之一，尚不能断言为全部原因。这个混合分布未经训练适配。

实验断点包含方法、区域和强度签名，与普通推理断点隔离。`scripts/check_detail_conditioning.py` 检查原特征不被修改、区域外 token 不变、非法选区被拒绝和断点不能混用。

## 真正微调的下一步

以下是后续训练设计，**不是已经训练或已验证的训练程序**。

1. 先准备少量高质量配对样本检验流程：有使用权的三维网格、对应照片/相机参数、前景遮罩，以及眼、鼻、耳等重点区域。人物与猫需要各自覆盖；同一个对象的不同视角不能拆到训练集和验收集两边。
2. 先验证几何自编码器能否重建这些目标网格。如果它已经丢失关键结构，单改图像条件或 DiT 无法完全补回，需要改 VAE 表示或使用局部模型。
3. 再做冻结 DINO/VAE、训练局部条件适配层或 DiT 低秩参数的对照；重点区域之外同时约束整体轮廓、比例及表面法线，避免只让五官纹理变多却把人改样。更密的三角面不是训练目标。
4. Turbo 的五步生成依赖其蒸馏方式。直接用普通 flow-matching 损失微调，不代表五步推理仍有效；需要与匹配的教师/蒸馏目标联合验证，或明确改为较多步的独立模型版本，不能替换现有 Turbo 后继续冒用原验证记录。
5. 验收使用未见过的对象与固定视角，检查局部几何误差、法线、轮廓、相似度、封闭性、薄壁和实际切片。需要实物尺寸/扫描真值，才能报告毫米级还原误差。

[腾讯官方 2.1 形状训练说明](https://github.com/Tencent-Hunyuan/Hunyuan3D-2.1/blob/main/hy3dshape/README.md) 提供数据准备和 DiT 训练示例。注意它的示例架构及检查点**并非当前部署的 2mini Turbo**，不能仅因为配置名含 mini 就直接套用。本轮核对的示例使用 DINO Large、4096 个潜变量及不同的 DiT 结构；本机 Turbo 使用 DINO Giant、3072 个潜变量。训练适配和资源用量需要另做验证，不能把本机能推理当成训练已经可行。

参考：[官方数据预处理说明](https://github.com/Tencent-Hunyuan/Hunyuan3D-2.1/blob/main/hy3dshape/tools/README.md)、[官方示例配置](https://github.com/Tencent-Hunyuan/Hunyuan3D-2.1/blob/main/hy3dshape/configs/hunyuandit-mini-overfitting-flowmatching-dinol518-bf16-lr1e4-4096.yaml)、[Mini 模型说明](https://huggingface.co/tencent/Hunyuan3D-2mini)。
