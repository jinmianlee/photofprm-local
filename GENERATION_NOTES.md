# 新照片生成与耗时排查

2026-09-08 的实际故障不是混元没有生成出模型：新上传照片的三维形状已经生成，随后更新 `status.json` 时发生 Windows `WinError 5`，导致整个任务被标记失败。日志只能证明文件替换被拒绝，不能据此认定是杀毒软件拦截。

已修正状态文件的读写：每次写入使用独立临时文件，遇到短暂占用会有限重试；持续失败仍明确报错。非关键进度显示失败不再中止有效推理，进度不再在网格完成时回退到 10%。同时修正服务查询状态和任务完成之间的竞争，避免把刚完成的任务误记为失败。

失败任务 `3405213da98545ae88136d308f690e6b` 已恢复完成。恢复时校验照片、生成参数和已有网格，没有重跑已花费 286.95 秒的形状推理；约 30 秒完成了上色、分色和导出。

## 如何避免重复等待

- 新照片需要重新提取图像特征、推理三维形状和提取网格。本机形状推理已使用 Intel Arc / XPU；整个流程仍有 CPU 和文件处理步骤。
- 只是调整尺寸、颜色数量、分色间距或照片颜色对齐时，在已完成任务上点“用当前参数重新分色”，无需重新生成形状。
- 如果任务中断且保留了断点，可点恢复按钮。完整原始形状校验通过时，直接继续打印处理；只完成部分推理时，从相应步骤恢复。
- 新照片不会套用旧照片的模型。缓存必须匹配照片、权重、采样设置和网格文件。
- 界面现在显示实际已用时间，完成后可展开阶段耗时。百分比按处理阶段划分，不能直接据此推算剩余时间。

## 速度与精细度

耗时包括背景去除、Python/模型环境初始化、权重校验及加载、照片特征提取、形状推理、高密度表面采样、实体布尔分色和导出。每次加载使用约 3.82 GB 官方权重；集成显卡还需要对注意力计算分块，以满足单次内存分配限制。

本次修复保持 5 步 Turbo、3072 个形状潜变量、255 网格提取分辨率，默认最长边 95 mm、4 色、0.8 mm 分色边界采样。模型加载优化通过直接装入官方张量，省去会被覆盖的随机参数初始化，不通过降低这些设置提速。实测记录和优化前后结果校验保存在 `data/new-photo-efficient-validation.json`。

同一张新照片经上传接口完整运行两次：原加载方式总计 365 秒，直接装载方式总计 343 秒（约 5 分 43 秒）。加载阶段从 35.78 秒降至 12.08 秒；优化后照片特征提取 31.73 秒、形状推理 126.14 秒、表面细化 58.84 秒，实体分色与导出 56.52 秒。其他阶段和机器负载也会波动，不能把加载阶段约三倍的速度差称为整个流程快三倍。

优化前后以及原失败任务的前景 PNG、最终形状潜变量和原始 PLY 的 SHA-256 全部相同。优化任务的 3MF、文件包、预览和五个 STL 均通过实际本地下载检查；五个 STL 重新读入后全部封闭。34 项回归测试通过，包含真实 Windows 文件占用复现；修改后的相关 16 项测试和实际调度器断点一致性检查也通过。

本机新照片完整生成仍需数分钟，时间随主体复杂程度、其他程序的资源占用及设备状态变化。生成流程通过不等于照片相似度或颜色对齐已验收；不同照片仍需要旋转检查，并视情况调整颜色对齐后重新分色。

修复没有更改杀毒、证书、驱动、系统执行策略或模型来源。
