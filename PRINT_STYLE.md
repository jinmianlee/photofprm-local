# 纯色打印与形状细节

当前混元 Mini Turbo 负责生成三维形状，上色由本地照片处理完成。原来的照片投影把阴影和高光一并带入分色，浏览器灯光又加了一层视觉明暗。这些问题不能仅靠增加混元推理步数解决。

## 已加入的设置

0.3 的猫照片对照和未达标形状问题见 [CAT_QUALITY.md](CAT_QUALITY.md)。默认五色，保留深色眼鼻标记；支持照片/原始模型对应点校准及小材料孤岛合并。预览材料已正确进行 sRGB/线性 RGB 转换。提高步数仍未明显改善猫脸比例。

- **纯色打印 · 抑制阴影**：按颜色比例与较低权重的明度聚类，合并小片明暗噪声；保持每个分区使用确定的材料颜色。真正的深色花纹与阴影仍可能混淆，这不是语义部位识别或神经去光照模型。
- **自选材料色**：先生成分区，再选择各区域的耗材颜色并重新分色。新照片会清除旧照片的材料色覆盖，避免错用旧区域编号。取色和实际耗材颜色不是同一回事。
- **纯色预览**：默认关闭预览灯光，以便观察材料边界；关掉此开关可查看带灯光的立体细节。该开关只影响显示。
- **形状细节 255 / 383**：浏览器新任务默认精细 383，表面采样更密，耗时也更长。已经生成的单图模型可从已保存的形状数据重新提取，不重复进行图像特征提取和五步形状推理。
- **照片颜色对齐**：纯色模式可按轮廓估计视角；这不是五官关键点配准，必要时仍需手动调整。

形状数据和官方权重保持原样。本次调整的是表面提取、上色、分色和预览方式，没有把已经生成的三角面简单细分后充当模型获得了新能力。

## 本机实际结果

最新人物照片：标准模型原始网格 261,230 面，精细提取 590,144 面。整理底面后的整体模型由 249,316 面增加至 562,848 面。两次使用完全相同的形状潜变量；383 提取耗时 176.56 秒，未重新运行五步形状推理。

三色示例选择了浅灰 `#BEC2CC`、深色 `#292A32`、肤色 `#D0AA93`。区域自动生成，三个最终色值由本地选择作为耗材示例，不能把它们称为 AI 自动恢复出的真实材质颜色。

该示例和四色自动取色版本均通过本地 API 完整运行；全部分色 STL 以及整体 STL 重新读取后封闭且法线一致，文件包和 3MF 可下载。最初的四色实验出现了不封闭色块并被阻止导出；减少明暗碎块后的三色、四色流程均完成验证。36 项回归测试通过，包含阴影梯度合并且保留黑白细节、重提取时保持照片断点对应关系。

- 实际三维文件对比：`data/flat-print-comparison.png`，上下两行各自使用相同相机和灯光，未进行图片润色。
- 三色任务：`data/b646b111b452486890a75596e75a5b4a/`
- 三色验证：`data/flat-print-validation.json`
- 四色验证：`data/flat-four-validation.json`

未针对这版人物模型进行实物打印；导出封闭不等于支撑、薄壁或人物相似度已经验收。

## 调混元能改善到什么程度

表面提取分辨率更高可以让已有轮廓和边缘更细致，不能补回形状生成阶段遗漏或推断错误的五官、手指与背面。Mini 是较小的形状模型；若目标是明显更高的相似度，后续需要比较更强的形状模型、补充多视角输入，或针对目标风格准备图片与高质量三维网格配对数据进行微调。单纯把 Turbo 的五步改成很多步不能保证细节提升。

官方资料：[Mini 模型说明](https://huggingface.co/tencent/Hunyuan3D-2mini)、[Mini 标准模型示例](https://github.com/Tencent-Hunyuan/Hunyuan3D-2/blob/main/examples/shape_gen_mini.py)。标准 Mini 示例的推理步数不应直接当作 Turbo 的推荐值。
