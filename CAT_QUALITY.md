# 猫照片相似度与分色 · 2026-09-17

目标是忠实、精细且能分色打印的猫模型。当前已改善材料分区和颜色位置，但 Mini Turbo 的脸形仍未达到用户要求；不能用封闭性或面数代替相似度验收。

## 已确认的问题和修复

- 原来只有三种相近棕色，深色眼睛与粉棕鼻子被普通聚类吞并。现在保留少量深色标记，默认五色；二维碎点清理门槛从前景的 1.2% 降至 0.02%，避免抹掉额头花纹。
- 单凭轮廓估计角度会使五官颜色错位。提供两眼、鼻尖三对对应点，由照片归一化坐标与实际原始模型坐标拟合仿射投影。共线、反向或病态标记拒绝使用。它不修正五官的三维形状。
- 未见表面使用主材料色，避免把眼鼻色块延续到背后；单图无法恢复真实背面花纹。
- 3MF 色值为 sRGB，glTF 材料因子为线性 RGB。预览现在正确转换，避免相同材料在页面中异常偏浅。
- 分色内部切面会影响自动平均的显示法线。预览在验证属于原外表面的顶点上插值原始表面法线，避免虚假的凹缝光影；只改预览法线，不改打印几何。
- 通过表面材料种子和距离场建立内部材料分区，再仅在材料边界带查询原始连续表面样本。保持输入外表面，通过实体布尔运算输出封闭部件；没有用体素壳代替原形状。
- 容积低于设定阈值立方的碎色区域尝试并入相邻材料。只改材料归属，整体外形与容积仍需检查。残留孤岛数量继续显示，不能保证细线或胡须可打印。

## 同图实际对照

| 形状方案 | 参数 | 实际结果 |
|---|---|---|
| Mini Turbo 基准 | 5 步，种子 12345，255 提取 | 可辨认猫头胸像，但脸形、眼鼻比例粗糙；原始 502,132 面不能解释为高保真 |
| Mini Turbo 更多步数 | 15 步，383 提取，相同种子 | 741 秒；原始 1,140,940 面。比例未明显改善，局部有条带，保留实验选项而不设为默认 |
| Standard mini 原始输入规格 | 512 潜变量，518 图像编码，30 步，255 提取 | 146 秒；清理退化面后封闭，但出现不合理的薄壳/弯曲身体，未替换默认引擎 |
| Standard mini 扩大规格 | 1024 潜变量，1022 图像编码，30 步 | 884 秒后无有效零交叉表面；该配置不属于原训练规格，未启用 |
| Hunyuan3D-Shape-2.1 | 官方固定权重和源码，4096 潜变量，50 步，种子 1234 | 1269 秒；370,448 面、单一封闭实体。折耳、眼眶、鼻梁和口鼻明显优于 Mini，已作为网页可选“2.1 精细”；肩背突起和背面仍是推断 |

以上为各次运行时间，未做重复计时；没有统一的跨模型性能排名。

最新真实分色任务 `3554640a22dd4dd6af66f77c7390883e` 使用基准五步形状、95 mm 最长边、0.5 mm 分色间距、五色、三个配准点。浏览器实际点选和重新分色通过，约 175 秒；6,752,766 个网格点中 1,019,896 点需要边界精确查询。48 个微小区域合并，合计约 2.662 mm³；分体总容积误差约 1.16×10⁻⁹。整体与五个材料 STL 重载后均封闭、法线一致、正容积，文件包 CRC 正常；主体材料仍有两处微小孤岛。未完成这版的实物试打。

私有对照图 `data/cat-quality-current.png` 直接渲染实际 STL；几何对照使用整体 STL，避免分色内部面影响显示法线。原照、网格和对照图没有随仓库上传。`scripts/compare_cat_quality.py` 可用部署者自己的对应照片与输出目录复现。

## 2.1 安全验证入口

源码固定为 [腾讯官方 Hunyuan3D-2.1](https://github.com/Tencent-Hunyuan/Hunyuan3D-2.1) 的 `82920d643c0dc2f7bfd7255f45f62d386edfe60c`；权重仓库固定为 `0b94677654c57bb9a6b6845cd7b704ccf551d327`。只准备形状文件，不下载 PBR 纹理模型或编译 CUDA 绘制扩展。

| 文件 | 校验值 |
|---|---|
| `hunyuan3d-dit-v2-1/model.fp16.ckpt`，7,366,389,768 字节 | SHA-256 `6b519fc7242f78e9b5f47ea4d55668fe3d944a2d27332f4ca68d29a6ff603f5e` |
| `config.yaml` | SHA-256 `a9e9b66f0163a9b827d730633ac88f47fcc8e3071dcbb9ee12d88ef7537c5c6e` |

`scripts/compare_shape21.py` 先验证哈希和官方形状包未修改，再用 `torch.load(..., weights_only=True, mmap=True)` 加载官方检查点；不允许任意 pickle 类或自动下载。分别加载图像编码、形状推理、网格解码，用完整 key 的 query 分块注意力和串行 CFG 控制内存。阶段输出是实验原始网格，**不能直接当成已验收打印文件**。

官方整包下载停滞时，可用 `scripts/download_shape21.py` 从同一官方 HTTPS 入口作有界分段续传；严格验证 HTTP 字节范围和最终 SHA-256，校验完成前不生成可加载检查点。不关闭 TLS 验证或安全软件。分段缓存位于忽略的 `models/`，需要额外磁盘空间。

本机已完成官方权重同图实测。5 步种子 1234 只得到模糊团块；30 步种子 12345 得到带大平面的错误形状；50 步种子 1234 才得到可辨认猫头胸像。因此网页的 2.1 精细默认固定为 50 步和种子 1234，30 步只留作对照。省内存串行 CFG 与官方双样本批量 CFG 的 5 步潜变量逐元素相同，最大误差为 0。推理每 5 步保存照片、权重、步数、种子和 CFG 模式绑定的断点。

50 步网格经过五色、95 mm、0.5 mm 分色处理：整体与五个 STL 部件重载后均封闭、法线一致、正容积，ZIP CRC 通过；61 个微小材料区域已合并，主体材料仍有 2 个小孤岛。分体容积相对误差约 5.96×10⁻⁶。尚未完成实物试打，背面和肩部应在切片预览中重点检查。

[官方 2.1](https://github.com/Tencent-Hunyuan/Hunyuan3D-2.1) 标注形状约 10 GB 显存、形状模型约 3.3B 总参数。本机为 32 GB 内存的 Intel 集显，不属于官方 CUDA 验收路径。模型使用[腾讯自定义社区许可](https://huggingface.co/tencent/Hunyuan3D-2.1/blob/0b94677654c57bb9a6b6845cd7b704ccf551d327/LICENSE)，有地域、用途和分发限制；不是无限制的 MIT/Apache 开源许可。

## 验收重点

原照只有约 576×532 像素且存在模糊。换大模型、换种子、提高表面分辨率都不能保证恢复原片没有记录的细毛和后侧形状。需逐项检查圆脸轮廓、折耳、眼睛大小/间距、鼻尖和口鼻起伏，再检查分色边界与实际切片；毫米级还原误差需要实物尺寸或扫描真值。
