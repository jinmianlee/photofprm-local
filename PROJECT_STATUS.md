# 项目压缩交接 · 2026-09-16

## 当前交接（0.3，形状质量仍未达标）

用户最新要求：模型必须像原猫照、精细并完成实体分色；避免通过增加面数冒充相似度改善。授权继续本地模型测试和同步既有公开 GitHub，照片/权重/日志不上传，安全软件/TLS/驱动不改变。

已改善：默认五色/0.5 mm，保留稀少深色眼睛和鼻子、小花纹；不可见表面只用主材料色；三对照片/原始 GLB 点校准仿射投影，校准只改上色；原图对照与照片视角；新增步数5/10/15及种子，改变参数清除旧配准点并隔离断点；恢复不同种子/步数提前409拒绝。重新分色复制原始 GLB，便于后续校准。

分色使用表面种子距离场加材料边界带连续样本查询，保持源外表面和布尔容积检查。可合并低于阈值立方的碎色实体，残留仍报告。预览 GLB 材料正确转换为线性 RGB；分色边缘预览法线从验证属于原表面的点插值，STL几何不改。初次服务连接不再短暂显示故障提示。

真实任务 `3554640a22dd4dd6af66f77c7390883e`：浏览器点选三对对应点并重新分色通过；95 mm、五色、0.5 mm；175秒；48微小区域合并，仍有2小孤岛；整体+5 STL重载封闭/法线一致/正容积，ZIP CRC正常。猫脸比例仍未获用户验收，不声称精细扫描。实际源对比图`data/cat-quality-current.png`忽略、不上传。

真实失败/不佳对照：Turbo15步383提取741秒，比例未明显改善且有条带；标准Mini512潜变量/518编码/30步146秒，闭合但不合理薄壳身体；扩大到1024潜变量/1022编码无有效表面。五步仍为默认，不宣传这些实验为增强权重。

官方2.1已完成同图验证并接入网页可选精细模式：50步、种子1234耗时1269秒，370,448面、单一封闭实体，折耳和五官明显优于Mini；5步团块、30步错误大平面均未宣传为增强结果。推理每5步保存绑定断点。五色打印输出整体与五个部件均封闭/法线一致/正容积，仍有2个小材料孤岛，肩背和背面需要切片检查。固定SHA/版本和完整数据见`CAT_QUALITY.md`。

本轮回归56通过/1符号链接权限跳过，TypeScript/Vite构建已通过；新增法线回归通过，最新界面生产构建通过。公共远端0.2为`63e38c8bca3f2f13e6eda5d228d47773309001a1`，本地0.2提交`78fb6fb452586610fb157a14b6f29e4280c4719c`树相同而历史不同。后续同步保留origin/main为父提交，通过git commit-tree +快进push，不强推。

当前服务8765已启动LAN模式、访问码沿用；启动器需保持运行。最新API代码/构建若继续变化，应待正式任务完成后Ctrl+C正常重启。以前0.2记录如下，仅作为历史。

## 本轮交接（0.2）

用户要求：修复无法使用；导入、设置/生成、检查/导出分成独立步骤；提供历史任务、取消进程与删除任务文件；让本地模型可通过局域网浏览器使用；完善他人部署方式并同步公开 GitHub。

初步诊断：8765 未监听，模型文件与旧验收记录存在，单图 capability.ready 为真。旧启动器需持续运行；旧部署依赖本机私有猫照验收记录，缺少通用检查入口。网络访问按同一局域网、显式开启、配对后访问实现；模型仍在服务电脑运行，不是 GitHub Pages 云端推理。

GitHub：`https://github.com/jinmianlee/photofprm-local`（用户确认公开）。上次远端提交 `8f94e3bd9bf480638030a445857935e7f5552270`，与本地初始提交树一致；152 文件，照片/权重/环境不上传。后续应保留远端已有提交再更新。

已实现：三个步骤独立展示，可返回设置复用模型；独立“任务与文件”页面列出全部正式任务、筛选、文件清单/占用、取消、停止后确认删除。删除仅接受服务生成的 32 位任务编号，拒绝目录链接；用户原有任务未清理。Windows 使用 Job Object 管理 worker 与 Conda 子进程，worker 在加载模型前登记进程组，取消等待整个组停止。

联网：`启动局域网.cmd` 开启所有本机网卡地址，客户端通过访问码配对；未配对 API/文件请求拒绝，同源保护保留。默认本机启动不对外监听。HTTP 局域网仅供可信私有网络；没有公网 HTTPS、多用户隔离或云推理。摄像头受浏览器安全上下文限制，手机可拍照后上传。

部署：`photoform.json` 配置 AI Conda、模型、源码、抠图目录；旧电脑默认目录兼容。`configure_engine.py` / `检查模型环境.cmd` 执行 `verify_engine.py`，校验官方文件与版本，导入 pipeline，比较真实 XPU 注意力与 CPU 参考结果。检查缓存绑定本机路径/文件信息，替代私有猫照历史验收依赖；不声称质量验收。`setup_turbo.py` 提供固定 Intel XPU 环境、官方源码/权重安装和分阶段重试；全新机器下载安装尚未现场验收。

本轮证据：48 pytest 通过，1 个符号链接创建测试因账户权限跳过；TypeScript/Vite 构建通过。真实父子进程停止测试通过。实际 API 网格生成、取消照片任务、删除后 404、原任务保留通过。局域网 IP 未配对 401 / 配对后 200 通过。猫照片新任务 `a68b58a2fda64e73906001d41321f922` 完成，353 秒，60 mm、三色；下载 ZIP CRC 正常，整体 + 3 个 STL 重载后封闭、法线一致、正容积。证据仅在 `data/v02-smoke.json` 与 `data/v02-management-live.json`。另用浏览器真实文件选择器验证导入→设置→生成→导出。

继续开发定位：`components/task-manager.tsx`、`lib/api.ts`、`backend/process_tree.py`、`backend/runtime_config.py`、`scripts/configure_engine.py`、`scripts/verify_engine.py`、`scripts/setup_turbo.py`。启动服务必须持续运行；网络连接本身不会自动启动模型后端。

继续开发时先读本文件；详细过程仅在排查相关问题时查阅。

## 目标与约束

Windows 本机浏览器上传照片/拍照，生成可识别的三维形状，减少阴影渐变，划分颜色实体，导出供切片和 3D 打印使用的 STL / 3MF。用户选用混元 2mini；不需要 Ollama。照片留在本机，不关闭杀毒、不添加白名单、不绕过证书校验、不调整显卡驱动。早期杀毒删除的文件未能确定，不能把后续故障都归因于杀毒。

## 当前可用

- React / Vite / Three.js 界面，FastAPI 本地服务 `127.0.0.1:8765`。
- Hunyuan3D-2mini **Turbo**，Miniconda / PyTorch 2.6.0+xpu，Intel Arc 核显；5 步、种子 12345、3072 个形状潜变量。不是 CPU 日常推理，也没有训练新权重。
- 单图整个主体或框选部位独立生成；原图保留，选区短边至少 256 像素。另保留 COLMAP/OpenMVS 多图重建与已有彩色网格导入。
- 纯色模式默认 3 色，可改材料色；默认关闭预览灯光。颜色来自本地照片处理，未部署混元纹理模型。
- 默认 383 表面提取，可选 255；已有形状可复用潜变量重提取。分色通过实体布尔运算，保留外表面；导出前检查封闭、法线和容积。
- 小孔修复默认关闭；单图开启后仅补受限的小三角/四边形缺口，不改已有顶点。

## 已解决的问题与证据

| 问题 | 实际处理 / 结果 |
|---|---|
| 新照片生成完成后报 WinError 5 | 独立临时文件、有限时间重试；非关键进度写入不终止推理，修复进程退出状态竞争 |
| 失败后从头重算、速度慢 | 照片与参数绑定断点，校验并复用原始网格；直接赋值加载官方权重，结果曾与原加载器逐字节一致 |
| 光影被当成多种材料 | 降低明度参与分色，合并明暗碎块，支持人工指定耗材色；仍可能混淆真实花纹与阴影 |
| 提高面数仍不精细 | 255→383 使用相同潜变量，不能补回遗漏五官；不再将面数当作细节质量分数 |
| 整图人物五官粗糙 | 放大头部特征再混合的实验改变脸形，未启用；框选头部独立生成可见结构更清楚，已接入界面 |
| 独立头部存在微孔 | 首次阻止导出；开启修复后补 1 个三角面，最长边约 0.509 mm，复用原网格 49 秒完成三色导出 |

最后一轮 **42 项回归测试通过**，前端 TypeScript 编译/生产构建通过，特征与断点隔离检查通过。最新独立头部约 69.816 × 72.308 × 95 mm，整体与 3 个材料 STL 下载后均封闭、法线一致、正容积，3MF/ZIP 可读取。原猫模型曾在 Bambu Studio 2.7 完成四部件导入与离线切片；测试打印机配置不是用户确认的设备。

## 未完成及下一步

**尚未达到整身高精度五官，也没有头身自动融合、语义分件、装配榫/间隙、薄壁完整检测、实物打印或毫米级还原精度验收。** 单张照片的背面由模型猜测；眼镜附近仍有模糊/碎边，分色存在小孤岛。通用 3MF 的颜色不一定自动变成切片器耗材颜色，需确认槽位。

真正微调应先准备有使用权的照片与高质量三维网格配对数据，先验证 VAE 能否保留目标细节，再对局部条件或 DiT 做训练和独立样本验收。官方 2.1 的训练示例与当前 Mini Turbo 架构/蒸馏方式不同，不能直接套配置。用户表示可接手训练，但目前没有可交付的已训练增强权重。

## 定位文件

- 入口/流程：`backend/app.py`、`single_photo.py`、`single_photo_turbo.py`、`worker.py`。
- 形状/缓存：`cpu_volume.py`（计算使用潜变量所在 XPU）、`bounded_attention.py`、`model_loading.py`、`shape_cache.py`、`inference_checkpoint.py`。
- 打印/颜色：`geometry.py`、`photo_color.py`、`print_colors.py`；框选：`photo_region.py`、`components/photo-region.tsx`。
- 详细说明：`GENERATION_NOTES.md`、`PRINT_STYLE.md`、`DETAIL_EXPERIMENTS.md`、`MODEL_COMPARISON.md`。
- 本机证据（不上传）：`data/region-api-validation.json`、`data/detail-head-only/`、`data/flat-print-validation.json`、`data/new-photo-efficient-validation.json`。
- 最新本机任务：`432ab64605f84744b6c22f0db82c10f8`。实验特征混合仅在 CLI 中，签名与正常断点隔离，不能误设为默认质量增强。

仓库保存源码、固定依赖和这些结论；`data/`、照片、网格、权重、第三方工具和运行环境不会随仓库上传。没有样本文件不代表其历史验证已经在新机器上重跑。
